import { useEffect, useState } from "react";

export function Friendshipbutton() {
    //will need to receive some props
    //otherProfile component
    //this.props.match.params.id from the otherProfile component
    //use a useEffect to figure out what is the relatiionship between the otherpfile we are viewving nd us the person looking at the profile
    //based on that we aill set the button text upon the initial mount if the component
}
